export interface IExperience {
    id: string
    created_at: string
    company: string
    position: string
    duration: string
    description: string
    about_id: string
    is_public: boolean
    user_id: string
}